# VoiceTaskSystem

Информационная система для управления задачами на основе голосового ввода.

## 📌 Описание

**VoiceTaskSystem** — это система для управления задачами, ориентированная на голосовой ввод.  
Система предоставляет REST API для:

- аутентификации и авторизации пользователей,
- управления задачами (создание, обновление, удаление, получение),
- интеграции с сервисом обработки голосовых команд.

На текущий момент проект находится в активной разработке, часть API реализована, часть описана в спецификации.

**Frontend** (пользовательский интерфейс) планируется как отдельное приложение, которое будет использовать REST API для взаимодействия с сервером.

## 🧩 Архитектура

Проект построен на основе многоуровневой архитектуры:

- **Backend (серверная часть)** — ASP.NET Core Web API  
- **Application слой** — бизнес-логика, CQRS, MediatR  
- **Infrastructure слой** — работа с базой данных, логированием и внешними сервисами  
- **Persistence** — PostgreSQL  

Система поддерживает контейнеризацию через Docker и мониторинг:

- **Prometheus / Grafana** — сбор и визуализация метрик  
- **ELK Stack (в разработке)** — централизованное логирование  

---

## 📄 REST API

Полная спецификация REST API описана в отдельном файле:

[docs/API.md](docs/API.md)

> Спецификация используется как контракт между backend и frontend.  
> Не все эндпоинты реализованы на текущий момент; реализация идёт поэтапно.

---

## 🔌 Взаимодействие компонентов

- **Task Manager** — основной backend-сервис  
- **Speech Processing Service** — сервис обработки голосовых команд  
- **PostgreSQL** — база данных  
- **Prometheus / Grafana** — метрики и дашборды  
- **ELK Stack** — централизованное логирование (планируется)  
- **Frontend** — пользовательский интерфейс (планируется)  

---

## ⚙️ Запуск проекта

Для локальной разработки используется Docker Compose:

```bash
docker compose up -d --build
```

- В docker-compose предусмотрены профили (`profiles`) для разных окружений:
    - в CI/CD можно исключить Prometheus и Grafana
    - в локальной разработке запускать все сервисы

---

## 📊 Логирование и мониторинг
- **Логирование**: Serilog
- **Метрики**: Prometheus
- **Визуализация**: Grafana
- **Централизованные логи**: ELK Stack (планируется)

---

## 🚧 Статус проекта

🔧 В разработке

**Реализовано:**
- базовая архитектура backend
- аутентификация и авторизация
- часть REST API
- логирование и метрики

**Планируется:**
- полная реализация REST API
- разработка frontend-приложения
- интеграция централизованного логирования
- настройка CI/CD